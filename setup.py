from setuptools import setup, find_packages

setup(
    name="python app",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        "pytest",
        # add other dependencies here
    ],
)
